# 夕语第六批执行清单（MiMo 实现 → Claude 优化）

---

## 任务 T1：Prompt Inspector（frontend + backend）

### 基础实现（MiMo 做）

**后端**：新建 `sakura_backend/api/debug.py`：

```python
"""调试模式：记录每次发送给 LLM 的完整 prompt"""
from fastapi import APIRouter
from pydantic import BaseModel

router = APIRouter(prefix="/debug", tags=["debug"])

# 内存缓冲区，存最近10条 prompt
_prompt_history = []

def log_prompt(prompt: str):
    _prompt_history.append(prompt)
    if len(_prompt_history) > 10:
        _prompt_history.pop(0)

@router.get("/prompts")
async def get_recent_prompts():
    return {"prompts": _prompt_history}
```

在 `sakura_backend/core/llm_client.py` 的 `call_llm()` 中记录：

```python
# 在发送请求前记录
try:
    from api.debug import log_prompt
    log_prompt(prompt)
except ImportError:
    pass
```

**前端**：在设置页面添加调试面板

```vue
<!-- DebugPanel.vue 新建 -->
<template>
  <div class="debug-panel" v-if="showDebug">
    <h3>最近 Prompt</h3>
    <pre v-for="(p, i) in prompts" :key="i">{{ p }}</pre>
    <button @click="refresh">刷新</button>
  </div>
</template>
```

**涉及文件**：
- `sakura_backend/api/debug.py`（新建~20行）
- `sakura_backend/core/llm_client.py`（~5行）
- `sakura_frontend/src/components/DebugPanel.vue`（新建~30行）

**验收标准**：
- 开启调试模式后能看到每次发送的完整 prompt
- 不开启时不影响性能

---

## 任务 T2：Token 计数器（backend + frontend）

### 基础实现（MiMo 做）

新建 `sakura_backend/core/token_counter.py`：

```python
"""简易 token 计数器（基于字符估算，不用 tiktoken 省依赖）"""
def estimate_tokens(text: str) -> int:
    """估算 token 数：中文约1.5字符/token，英文约4字符/token"""
    chinese_chars = sum(1 for c in text if '一' <= c <= '鿿')
    other_chars = len(text) - chinese_chars
    return int(chinese_chars / 1.5 + other_chars / 4)

def count_prompt(prompt: str) -> dict:
    """统计 prompt 的 token 消耗"""
    return {
        "total_tokens": estimate_tokens(prompt),
        "characters": len(prompt),
    }
```

在 WebSocket 响应中添加 token 计数：

```python
# 在 chat_session.py 的 _process_reply 中
from core.token_counter import estimate_tokens
tokens_used = estimate_tokens(reply_text)
await ws.send_text(json.dumps({
    "type": "token_usage",
    "tokens": tokens_used
}))
```

**涉及文件**：
- `sakura_backend/core/token_counter.py`（新建~15行）
- `sakura_backend/api/chat_session.py`（~3行）

**验收标准**：
- 每条消息显示 token 数
- 数字基本合理（和实际偏差不超过20%）

---

## 任务 T3：聊天代码执行（backend）

### 基础实现（MiMo 做）

新建 `sakura_backend/core/code_runner.py`：

```python
"""安全代码执行：在隔离环境中运行代码片段"""
import subprocess
import tempfile
import os

def run_python(code: str, timeout: int = 5) -> str:
    """运行 Python 代码片段（超时5秒）"""
    with tempfile.NamedTemporaryFile(mode='w', suffix='.py', delete=False) as f:
        f.write(code)
        f.flush()
        try:
            result = subprocess.run(
                ['python', f.name],
                capture_output=True, text=True,
                timeout=timeout
            )
            if result.returncode == 0:
                return result.stdout[-1000:]  # 截取最后1000字符
            else:
                return f"错误：{result.stderr[-200:]}"
        except subprocess.TimeoutExpired:
            return "错误：执行超时（5秒）"
        finally:
            os.unlink(f.name)
```

在 `core/mcp_tools.py` 注册：

```python
"run_code": {
    "description": "执行 Python 代码，code: 要执行的代码",
    "handler": lambda code: code_runner.run_python(code)
}
```

**涉及文件**：
- `sakura_backend/core/code_runner.py`（新建~30行）
- `sakura_backend/core/mcp_tools.py`（~5行）

**验收标准**：
- 可执行简单 Python 代码
- 超时5秒自动终止
- 不暴露文件系统

---

## 任务 T4：热插拔插件系统（backend）

### 基础实现（MiMo 做）

新建 `sakura_backend/core/plugin_loader.py`：

```python
"""插件系统：从 plugins/ 目录热加载第三方扩展"""
import os
import importlib.util
import logging

logger = logging.getLogger(__name__)

_plugins = {}

def discover_plugins():
    """扫描 plugins/ 目录，加载所有插件"""
    plugin_dir = os.path.join(os.path.dirname(__file__), '..', 'plugins')
    os.makedirs(plugin_dir, exist_ok=True)
    
    for f in os.listdir(plugin_dir):
        if f.endswith('.py') and not f.startswith('_'):
            name = f[:-3]
            path = os.path.join(plugin_dir, f)
            try:
                spec = importlib.util.spec_from_file_location(name, path)
                mod = importlib.util.module_from_spec(spec)
                spec.loader.exec_module(mod)
                if hasattr(mod, 'register'):
                    _plugins[name] = mod
                    logger.info(f"[插件] 已加载: {name}")
            except Exception as e:
                logger.warning(f"[插件] 加载失败 {name}: {e}")

def get_plugins():
    return list(_plugins.keys())

def call_plugin(name: str, *args, **kwargs):
    mod = _plugins.get(name)
    if mod and hasattr(mod, 'run'):
        return mod.run(*args, **kwargs)
    return None

# 启动时自动加载
discover_plugins()
```

新建 `plugins/example.py` 作为示例插件：
```python
"""示例插件"""
def register():
    return {"name": "示例插件", "version": "1.0"}

def run(params):
    return f"插件收到: {params}"
```

**涉及文件**：
- `sakura_backend/core/plugin_loader.py`（新建~40行）
- `sakura_backend/plugins/example.py`（新建~8行）

**验收标准**：
- plugins/ 目录下的 .py 文件自动加载
- 可调用插件功能
- 插件出错不影响主程序

---

## 任务 T5：信息采集（backend）

### 基础实现（MiMo 做）

新建 `sakura_backend/core/info_collector.py`：

```python
"""信息采集：RSS 订阅 + 新闻抓取"""
import feedparser
import requests
from datetime import datetime
from core.db import get_conn

def fetch_rss(url: str) -> list:
    """抓取 RSS 订阅"""
    try:
        feed = feedparser.parse(url)
        items = []
        for entry in feed.entries[:10]:
            items.append({
                "title": entry.title,
                "link": entry.link,
                "summary": entry.summary[:200],
                "published": entry.get("published", datetime.now().isoformat())
            })
        return items
    except Exception as e:
        return [{"error": str(e)}]

def fetch_news(query: str) -> list:
    """简单新闻抓取"""
    try:
        from duckduckgo_search import DDGS
        with DDGS() as ddgs:
            results = list(ddgs.text(query, max_results=5))
            return [{"title": r["title"], "body": r["body"]} for r in results]
    except Exception:
        return []
```

**涉及文件**：
- `sakura_backend/core/info_collector.py`（新建~35行）

**验收标准**：
- 可抓取 RSS 内容
- 搜索结果入库可查

---

## 任务 T6：推送通知（frontend + backend）

### 基础实现（MiMo 做）

**后端**：新建 `sakura_backend/api/push.py`：

```python
"""Web Push 推送通知"""
from fastapi import APIRouter
from pydantic import BaseModel

router = APIRouter(prefix="/push", tags=["push"])

_subscriptions = []

class PushSubscription(BaseModel):
    endpoint: str
    keys: dict

@router.post("/subscribe")
async def subscribe(sub: PushSubscription):
    _subscriptions.append(sub.dict())
    return {"status": "ok"}

@router.post("/send")
async def send(title: str, body: str):
    # 简化实现：记录到日志，实际推送需 Web Push 协议
    import logging
    logging.getLogger(__name__).info(f"[推送] {title}: {body}")
    return {"status": "ok", "sent": len(_subscriptions)}
```

**前端**：注册 Service Worker
```javascript
// public/service-worker.js 新建
self.addEventListener('push', function(event) {
  const data = event.data.json()
  self.registration.showNotification(data.title, {
    body: data.body,
    icon: '/icon.png'
  })
})
```

**涉及文件**：
- `sakura_backend/api/push.py`（新建~30行）
- `sakura_frontend/public/service-worker.js`（新建~10行）

**验收标准**：
- 注册推送正常
- 推送消息到达（需 HTTPS）

---

## 第六批交付验证

| 任务 | MiMo 完成 | Claude 优化 |
|------|-----------|------------|
| T1 Prompt Inspector | ⬜ | ⬜ |
| T2 Token 计数器 | ⬜ | ⬜ |
| T3 代码执行 | ⬜ | ⬜ |
| T4 插件系统 | ⬜ | ⬜ |
| T5 信息采集 | ⬜ | ⬜ |
| T6 推送通知 | ⬜ | ⬜ |
